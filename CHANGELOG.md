# 更新日志

本文件记录西瓜todo每个版本的改动内容。

---

## v3.0

本版本聚焦**主题系统**与**桌面小西瓜**两大块，并修掉了几个体验上的顽固问题。

### 新增

- **主题商店弹窗**：点标题栏 🎨 先弹出简洁菜单（快速切换），菜单里的
  「主题商店与 DIY…」进入卡片式商店。每张卡片带迷你待办预览，
  能真实呈现该主题的**背景渐变**效果，选中项有 ✓ 角标。
- **主题扩充至 19 套**：新增 深邃蓝 / 薄荷青 / 焦糖棕 / 玫瑰金 / 蜜桃汽水 /
  晚霞 / 极光夜 / 棉花糖 / 莫兰迪晨雾，其中多套为渐变背景。
- **DIY 配色专属弹窗**：可分别自定义 主色 / 副色 / 面板 / 卡片 / 文字 /
  副文字 / 边框 七项颜色。
  - **实时预览**：每选一个颜色，主界面立即整体套用，所见即所得。
  - **取名保存**：可为配色命名并保存成自己的主题，支持保存**多套**，
    保存后会出现在主题商店和 🎨 快捷菜单里。
  - **取消即还原**：点取消自动恢复到打开前的配色，不留痕迹。
- **小西瓜表情与动作**：新增 normal / blink / cheerful / surprised /
  sleepy / love / dizzy / cheer 共 8 种表情；加入**立体转身**动画
  （横向 cos 挤压模拟 3D 转身）、弹跳，以及花瓣 / 星星 / 爱心粒子特效。
- **完成待办庆祝**：完成待办后小西瓜会撒花并随机弹一句庆祝台词，
  例如「哇你太棒了！今天已完成 N 个待办 🎉」。

### 修复

- **悬停台词疯狂刷新**：光标停在小西瓜右上角时台词会飞快切换。
  原因是气泡被定位在西瓜右上角**之上**，与西瓜的悬停判定区重叠，
  两者反复互相触发 enter/leave，每次都随机换一句台词形成死循环。
  现改为：悬停判定区只算西瓜本体（排除四周透明留白），气泡定位到本体
  外沿之外，并改用定时轮询 + 状态跳变驱动，状态没变绝不换台词。
- **鼠标碰到西瓜不出台词，要先点一下才有反应**：小西瓜是「不抢焦点」窗口，
  在 macOS 上未激活时收不到 enter/leave 事件。现补上 `WA_Hover` 与鼠标跟踪，
  并用轮询兜底，不依赖窗口激活状态，鼠标一碰即出。
- **DIY 时西瓜挡住弹窗点不了「完成」**：小西瓜是置顶窗口，会盖在模态弹窗上，
  正好压住按钮时导致点击无效。现在打开主题商店期间自动收起小西瓜，关闭后恢复。
- **点击小西瓜后它会消失一会儿**：主窗口置顶时盖住了小西瓜。
  现在唤起主窗口后会把小西瓜重新提到最前。
- **程序意外退出**：转身动画里的局部变量 `sx` 覆盖了同名的 SVG 尺寸换算函数
  `sx()`，导致 `TypeError` 并崩溃；已改名为 `turn_sx`。
- **强提醒弹窗在 macOS 上显示异常**：无边框 + 半透明 + 自绘背景的组合
  在 macOS 上产生重影（Windows 正常）。改用「透明外壳 + 不透明圆角面板」
  结构，跨平台表现一致；同时修正数字输入框的上下对齐。
- 气泡与西瓜的间距过大，现已贴近本体（保留 2px 安全间隙）。

### 调整

- **强提醒设置**：移除多余的「启用强提醒」勾选框，改为按实际设置自动判定
  （勾了悬浮提醒、或提前提醒/间隔提醒大于 0 即视为已启用）。
- **编辑按钮铅笔图标方向**：macOS 下做左右镜像，Windows 保持原样，
  以贴合各自平台的视觉习惯。
- 删除「摸鱼一时爽，一直摸鱼一直爽」这句鼓励文案。
- 默认提醒间隔为每 2 小时。

### 兼容性

- 旧版本保存的单套 `custom_theme` 配色会在启动时自动迁移为
  新的多套结构 `custom_themes`（沿用「自定义」这个名字），无需手动处理。

---

## v2.x 及更早

早期版本的主要能力：待办增删改查、四象限 / 优先级、番茄钟、
桌面悬浮小西瓜、强提醒、数据本地持久化、Windows / macOS 双端打包等。
