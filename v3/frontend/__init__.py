"""西瓜 todo 前端层（PySide6 桌面界面）。

前端只通过 :class:`backend.api.TodoBackend` 访问数据，不直接读写文件，
所有业务规则都在后端；这一层只负责「怎么显示」和「怎么交互」。

目录分工：

- ``native``：与操作系统打交道的窗口特效、全局快捷键
- ``theme``：配色方案、主题管理与样式表
- ``widgets``：可复用控件
- ``dialogs``：各类弹窗
- ``windows``：主窗口与桌面悬浮小西瓜
"""
