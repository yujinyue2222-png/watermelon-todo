# 桌面端运行依赖（Python >= 3.9）
PySide6>=6.5

# 说明：
# - backend/ 与 tests/ 只用标准库，跑测试不需要装 PySide6
# - server/ 只用标准库，部署到服务器不需要 pip install 任何东西
# - 打包时额外需要（本地/CI 打安装包用，日常开发不必装）：
#     PyInstaller>=6.0
#     Pillow>=10.0    # 仅 Windows 生成 .ico 时需要
